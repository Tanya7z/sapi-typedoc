import type { OnSearch } from '@rspress/core/theme';

/** Rspress 自定义搜索钩子占位（Task 8 实现 domainTags + searchBoost） */
export const onSearch: OnSearch = (_query, _matchedResult) => {
  // 占位：不改写默认匹配结果
};
